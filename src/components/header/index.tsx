import React from 'react'
import logoUrl from '@/images/logo.svg'

const Header = () => {
  return (
    <header>
      <img src={logoUrl} alt="John Newman" />
    </header>
  )
}

export default Header